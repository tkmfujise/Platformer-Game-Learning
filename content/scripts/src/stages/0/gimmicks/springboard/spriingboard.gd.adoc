---
title: res://src/stages/0/gimmicks/springboard/spriingboard.gd
---
:toc:

== src / stages / 0 / gimmicks / springboard / spriingboard.gd

=== Attached Scenes
* link:/scenes/src/stages/0/gimmicks/springboard/spriingboard.tscn[res://src/stages/0/gimmicks/springboard/spriingboard.tscn]



=== Code
```gdscript
extends Sprite2D

@onready var standby : bool = true

const FORCE := -2000

func hit(player: Player) -> void:
	#standby = false
	player.velocity.y = FORCE
	%AnimationPlayer.play(&'hit')


func _on_area_2d_area_entered(area: Area2D) -> void:
	if standby: hit(area.owner as Player)


func _on_animation_player_animation_finished(_anim_name: StringName) -> void:
	standby = true

```
