---
title: res://src/beginning/chapters/menu/photo/photo.gd
---
:toc:

== src / beginning / chapters / menu / photo / photo.gd

=== Attached Scenes
* link:/scenes/src/beginning/chapters/menu/photo/photo.tscn[res://src/beginning/chapters/menu/photo/photo.tscn]



=== Code
```gdscript
extends Sprite2D

@export var flicked: bool = false
@export var image : Texture2D : set = set_image


func set_image(img: Texture2D) -> void:
	%Image.texture = img


func flick() -> void:
	if not flicked: %AnimationPlayer.play("flick")


func pull() -> void:
	if flicked: %AnimationPlayer.play("pull")

```
